# 06 — Log de implementação (2026‑09‑17)

| Arquivo | Problema | Alteração | Motivo | Risco | Teste | Resultado |
| --- | --- | --- | --- | --- | --- | --- |
| `contents/ui/ProviderModel.qml` | `isHttpUrl` rejeitava URLs com espaço (QUrl "pretty decoded": `scope=openid profile email`) → toda navegação OAuth do Google era cancelada | Valida só esquema+host; `normalizeUrl` (entrada do usuário) continua sem espaços | Causa raiz 1 do login Google | Aceita mais URLs em `openExternalIfSafe` (continua só http/https) | unit (`isHttpUrl` com espaços; host com espaço rejeitado; ftp/mailto/about:blank rejeitados); widget: ChatGPT → Google carrega | PASS |
| `contents/ui/ProviderModel.qml` | `isAuthUrl` por substring (`accounts.google.com.evil.example`, `"clerk"`) | `authRules` com host exato/subdomínio + prefixos de caminho; heurística de caminho com âncoras | Segurança | Hosts de login não listados caem na heurística de caminho (só decide popup interno × externo) | unit (11 casos, 4 negativos de segurança) | PASS |
| `contents/ui/AuthPopup.qml` (novo) | Popups `window.open` eram convertidos em navegação da view principal (sem `opener`) | View auxiliar com o mesmo perfil; `openIn(view)`; `windowCloseRequested`; cabeçalho host/cadeado/✕; Esc; popups aninhados → navegador; permissões/certificados pela mesma política | Causa raiz 2 (Claude, Perplexity, qualquer OAuth por popup) | `renderProcessTerminated` dispara ao adotar a página → só fecha em Crashed/Killed; depois removido do critério | mock local (`AUTH_OK`, popup fecha); Claude → Google no popup | PASS |
| `contents/ui/WebView.qml` | `onNewWindowRequested` descartava o pedido; `request.action`/`IgnoreRequest` inexistentes no tipo | Política: Dialog/Window, `about:blank`, ou URL de auth → `AuthPopup`; `_blank` comum → navegador. `DestinationType` não é exposto ao QML → valores numéricos documentados com `??` | Causa raiz 2 | Popups comuns (compartilhar etc.) agora abrem dentro do widget | mock/Claude no widget; `enumDialog=undefined` confirmado no log | PASS |
| `contents/ui/WebView.qml` | Callback que chama `window.close()` na view principal ficava exibido | `onWindowCloseRequested: goHome()` | Sintoma de tela em branco | — | código | — |
| `contents/ui/WebView.qml` | Downloads: média simples, sem visto/velocidade/ETA/retry, `setProperty` ×6 a cada byte | Modelo único com `seen/speed/eta/sourceUrl/mimeType`; `downloadSummary` (Downloads.js); throttle 250 ms + flush; `openDownload`, `showDownloadInFolder` (FileManager1 via `Plasma5Support` executable, fallback pasta), `retryDownload`, `removeDownload(id)`, `markDownloadSeen`; notificação com ações; `downloadAttention()` | UX de downloads | Notificação por download (uma por conclusão) | funcional no widget: 2 Blobs → indicador → seen → oculto; `downloadCache` vazio | PASS |
| `contents/ui/Downloads.js` (novo) | lógica espalhada | funções puras testáveis | testabilidade | — | 24 casos unitários | PASS |
| `contents/ui/DownloadIndicator.qml` (novo) | botão permanente de Downloads | indicador temporário com anel de progresso (Shapes), badge ✓/!/n, animações curtas, `Accessible` | UX pedida | `Behavior` em propriedade `readonly` quebrava a instanciação (qmllint não detecta) → corrigido | qml6 instancia; widget | PASS |
| `contents/ui/DownloadPopup.qml` (novo) | barra fixa no rodapé | popup com lista, estados, %, velocidade/ETA, Pausar/Retomar/Cancelar/Abrir/Mostrar na pasta/Tentar de novo/Remover, pasta, limpar | UX pedida | — | widget (captura) | PASS |
| `contents/ui/DownloadBar.qml` | redundante | removida | UX pedida | usuários acostumados à barra | — | — |
| `contents/ui/Header.qml` | botões permanentes Olho e Downloads; overflow de 4 botões | removidos; `DownloadIndicator` temporário; `DownloadPopup` ancorado ao indicador ou ao ⋮; overflow só ⌂/🔍; `emptyDownloadSummary` quando não há WebView | UX pedida | — | widget | PASS |
| `contents/ui/ChatAIMenu.qml` | itens de overflow para Olho/Downloads | "Ocultar barra automaticamente" ✓ e "Downloads (n)" permanentes; sinal `downloadsRequested` | UX pedida | primeira gravação do arquivo falhou (modificado entre leitura e escrita) → regravado | widget | PASS |
| `contents/ui/main.qml` | barra auto‑oculta não revelava indicador | `Connections onDownloadAttention` → revela 3 s | UX | — | código | — |
| `contents/ui/settings/AppearanceSettings.qml`, `contents/config/main.xml` | toggles de botões inexistentes | removidos da UI; chaves mantidas (obsoletas) | compat | — | — | — |
| `tools/tests/downloads_test.qml`, `provider_checks.qml.in`, `run-unit-tests.sh` | cobertura | novos casos | — | — | 3 suites PASS | PASS |
| `locale/*` | strings novas | `.pot` 336 msgs, catálogos e `.mo` regenerados | i18n | traduções novas em inglês | `msgfmt --check` | PASS |
